# Copy this file to .env and fill in real values. Do NOT commit your real .env file.

# API authentication key used by the Star API
API_KEY=change-me-in-production

# Admin login for dashboard (use strong values in real .env)
ADMIN__USERNAME=admin
ADMIN__PASSWORD=changeme

# JWT settings (use strong key in production)
JWT__ISSUER=StarApi
JWT__AUDIENCE=StarApiAudience
JWT__KEY=dev-secret-key-change-in-production-1234567890
JWT__EXPIREMINUTES=60

# AWS / DigitalOcean Spaces configuration
# Access key and secret key for AWS S3-compatible storage
AWS__AccessKey=your-spaces-access-key
AWS__SecretKey=your-spaces-secret-key
# ServiceURL should be the endpoint URL: https://{region}.digitaloceanspaces.com
AWS__ServiceURL=https://sgp1.digitaloceanspaces.com
# Bucket name for storage
AWS__BucketName=cute33
# Optional: If you front your Space with a CDN (e.g., DO CDN, Cloudflare), set base URL
AWS__CdnBaseUrl=https://cute33.sgp1.cdn.digitaloceanspaces.com
# Usually keep this false unless you need plain HTTP for local testing via a proxy
AWS__UseHttp=false
